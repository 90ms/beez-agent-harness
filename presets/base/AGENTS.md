## Base preset

Discover build, test, lint, and formatting commands from project configuration.
Do not invent missing commands. Update `.harness/project.json` when the project
has stable commands or boundaries that future agents should follow.
