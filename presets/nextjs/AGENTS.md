## Next.js preset

- Follow the existing package manager and App Router or Pages Router structure.
- Keep server-only values out of client bundles.
- Verify user-visible changes at relevant responsive widths.
- Run the declared lint, test, and build commands after affected changes.
- Treat accessibility and hydration warnings as product defects.
