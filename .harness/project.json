{
  "schemaVersion": 1,
  "commands": {
    "test": "npm test",
    "check": "npm run check",
    "validate": "npm run validate",
    "evaluate": "npm run evaluate",
    "pack": "npm pack --dry-run"
  },
  "verification": {
    "required": ["test", "check", "validate"],
    "profiles": {
      "default": ["test", "check", "validate"],
      "migration": ["test", "check", "validate", "evaluate"],
      "security": ["test", "check", "validate", "evaluate"],
      "release": ["test", "check", "validate", "evaluate", "pack"],
      "performance": ["test", "check", "validate", "evaluate"]
    },
    "timeoutMs": 300000
  },
  "boundaries": [
    "Do not commit secrets or machine-specific configuration.",
    "Preserve unrelated user changes.",
    "Do not perform destructive or externally visible actions without authority.",
    "Keep the CLI runtime dependency-free.",
    "Keep Agent Skills concise and validate every SKILL.md before release."
  ]
}
